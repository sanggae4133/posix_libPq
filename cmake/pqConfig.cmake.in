@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(PostgreSQL REQUIRED)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/pqTargets.cmake")

check_required_components(pq)
